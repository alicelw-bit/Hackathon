name: eShopOnWeb Build and Test

on: [push, workflow_dispatch]

env:
  RESOURCE_GROUP: Hackathon
  LOCATION: westeurope
  TEMPLATE_FILE: infra/webapp.bicep
  SUBSCRIPTION_ID: 539fbb79-2988-4831-927f-e46e465c06b1
  WEBAPP_NAME: eshoponweb-webapp-Hackathon

jobs:
  buildandtest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: "8.0.x"
          dotnet-quality: "preview"

      - name: Build with dotnet
        run: dotnet build ./eShopOnWeb.sln --configuration Release

      - name: Test with dotnet
        run: dotnet test ./eShopOnWeb.sln --configuration Release

      - name: dotnet publish
        run: |
          dotnet publish ./src/Web/Web.csproj -c Release -o ${{ env.DOTNET_ROOT }}/myapp
          cd ${{ env.DOTNET_ROOT }}/myapp
          zip -r ../app.zip .

      - name: Upload artifact (webapp)
        uses: actions/upload-artifact@v5
        with:
          name: .net-app
          path: ${{ env.DOTNET_ROOT }}/app.zip

      - name: Upload artifact (bicep)
        uses: actions/upload-artifact@v5
        with:
          name: bicep-template
          path: ${{ env.TEMPLATE_FILE }}

#DEVELOPMENT 
  deploy_dev:
    runs-on: ubuntu-latest
    needs: buildandtest
    environment:
      name: Development
    steps:
      - name: Download artifact (webapp)
        uses: actions/download-artifact@v6
        with:
          name: .net-app
          path: .net-app

      - name: Download artifact (bicep)
        uses: actions/download-artifact@v6
        with:
          name: bicep-template
          path: bicep-template

      - name: Azure Login
        uses: azure/login@v2
        with:
          auth-type: SERVICE_PRINCIPAL
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy infra (Bicep)
        uses: azure/arm-deploy@v2
        with:
          subscriptionId: ${{ env.SUBSCRIPTION_ID }}
          resourceGroupName: "${{ env.RESOURCE_GROUP }}-dev"
          template: bicep-template/webapp.bicep
          parameters: "webAppName=${{ env.WEBAPP_NAME }}-dev location=${{ env.LOCATION }}"
          failOnStdErr: false

      - name: Publish Website to WebApp (CLI)
        uses: Azure/cli@v2
        with:
          inlineScript: |
            az webapp deploy \
              --name ${{ env.WEBAPP_NAME }}-dev \
              --resource-group "${{ env.RESOURCE_GROUP }}-dev" \
              --src-path .net-app/app.zip \
              --type zip
              
  deploy_stag:
    runs-on: ubuntu-latest
    needs: deploy_dev
    environment:
      name: Staging
    steps:
      - name: Download artifact (webapp)
        uses: actions/download-artifact@v6
        with:
          name: .net-app
          path: .net-app

      - name: Download artifact (bicep)
        uses: actions/download-artifact@v6
        with:
          name: bicep-template
          path: bicep-template

      - name: Azure Login
        uses: azure/login@v2
        with:
          auth-type: SERVICE_PRINCIPAL
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy infra (Bicep)
        uses: azure/arm-deploy@v2
        with:
          subscriptionId: ${{ env.SUBSCRIPTION_ID }}
          resourceGroupName: "${{ env.RESOURCE_GROUP }}-staging"
          template: bicep-template/webapp.bicep
          parameters: "webAppName=${{ env.WEBAPP_NAME }}-staging location=${{ env.LOCATION }}"
          failOnStdErr: false

      - name: Publish Website to WebApp (CLI)
        uses: Azure/cli@v2
        with:
          inlineScript: |
            az webapp deploy \
              --name ${{ env.WEBAPP_NAME }}-staging \
              --resource-group ${{ env.RESOURCE_GROUP }}-staging \
              --src-path .net-app/app.zip \
              --type zip
              
      - name: Test with the dotnet CLI
        run: dotnet test 'tests/IntegrationTests/*.csproj' --no-build

      - name: Test with the dotnet CLI
        run: dotnet test 'tests/FunctionalTests/*.csproj' --no-build


 
  deploy_prod:
    runs-on: ubuntu-latest
    needs: deploy_stag
    environment:
      name: Production
    steps:
      - name: Download artifact (webapp)
        uses: actions/download-artifact@v6
        with:
          name: .net-app
          path: .net-app

      - name: Download artifact (bicep)
        uses: actions/download-artifact@v6
        with:
          name: bicep-template
          path: bicep-template

      - name: Azure Login
        uses: azure/login@v2
        with:
          auth-type: SERVICE_PRINCIPAL
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy infra (Bicep)
        uses: azure/arm-deploy@v2
        with:
          subscriptionId: ${{ env.SUBSCRIPTION_ID }}
          resourceGroupName: "${{ env.RESOURCE_GROUP }}-prod"
          template: bicep-template/webapp.bicep
          parameters: "webAppName=${{ env.WEBAPP_NAME }}-prod location=${{ env.LOCATION }}"
          failOnStdErr: false

      - name: Publish Website to WebApp (CLI)
        uses: Azure/cli@v2
        with:
          inlineScript: |
            az webapp deploy \
              --name ${{ env.WEBAPP_NAME }}-prod \
              --resource-group "${{ env.RESOURCE_GROUP }}-prod" \
              --src-path .net-app/app.zip \
              --type zip              
